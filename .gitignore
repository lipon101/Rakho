node_modules/
dist/
.venv/
venv/
__pycache__/
*.pyc
db.sqlite3
db_preview.sqlite3
backend/db_preview.sqlite3
backend/db_scratch.sqlite3
.server_preview.log
.server.log

# The public medicine dataset the catalogue import caches locally. It is
# re-downloadable, so it never belongs in a clone.
backend/catalog_cache/
backend/staticfiles/
.DS_Store
.freebuff/
.buildtools/

# ── Android ──
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.iml
*.apk
*.aab
# Never commit signing material.
*.jks
*.keystore
android/keystore.properties

# ── Private source: never publish to the public repo ──
android/
src/
docs/
/index.html
package.json
package-lock.json
pnpm-workspace.yaml
vite.config.ts
postcss.config.mjs
*.tar.gz

# Firebase config (per-environment, never commit)
google-services.json
**/google-services.json

# Local admin-dashboard preview helpers (never commit)
backend/config/preview_settings.py
backend/seed_preview.py
